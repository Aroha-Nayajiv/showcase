# Architecture Overview

# Patient Intake System – Technical Design (Refined)

## 1. High‑Level Narrative (≈150 words)
The PatientIntake system is realized as a set of tightly scoped micro‑services deployed in Docker containers and orchestrated with Docker‑Compose for on‑premise, air‑gapped operation. All external traffic terminates at a FastAPI API Gateway that enforces TLS 1.3 (as defined in the project globals) and performs JWT‑based authentication. The UI is a React single‑page application served over HTTPS and communicates exclusively with the gateway via REST endpoints.

Business logic is split into four core services:

- **Auth Service** – issues and validates short‑lived JWTs, integrates with LDAP/Active Directory for role lookup (admin, clinician, front‑desk).  
- **Intake Service** – validates the structured intake form, applies field‑level encryption using libsodium, and persists encrypted payloads to PostgreSQL.  
- **PDF Generation Service** – renders a sanitized HTML template into a PDF via wkhtmltopdf, adds a dynamic watermark and timestamp before streaming the file.  
- **Audit Logging Service** – writes immutable audit records to an append‑only table; each record includes user ID, operation type, timestamp, and cryptographic hash of the payload.

All services write to the same encrypted PostgreSQL instance but enforce row‑level security (RLS) policies that map directly to the three roles. The architecture satisfies HIPAA technical safeguards (45 CFR 164.312) by ensuring data is encrypted in transit (TLS 1.3) and at rest (field‑level encryption + disk encryption).

## 2. Service Boundary Table
| Service | Responsibility | Dependencies | Success Events | Failure Events |
|---|---|---|---|---|
| **Auth Service** | Issue/validate JWTs; map LDAP groups to roles | LDAP/AD, JWT library | `auth.success` | `auth.failure` |
| **Intake Service** | Validate form payload; encrypt fields; store records | libsodium, PostgreSQL RLS | `intake.submitted`, `intake.error` | `auth.success` |
| **PDF Generation Service** | Render HTML → PDF; apply watermark & timestamp | wkhtmltopdf, HTML template store | `pdf.generated`, `pdf.error` | `auth.success`, `intake.submitted` |
| **Audit Logging Service** | Record immutable audit entries for every read/write/export | PostgreSQL audit table | `audit.recorded` | All other services |

## 3. API Contracts & Schemas

### 3.1 POST /api/v1/intake
**Purpose:** Submit a new patient intake record.

**Request Header**

- **Authorization**: Bearer <jwt-token>
- **Content-Type**: application/json

**Request Body Schema**

{
  "patient_id": "string",               // UUID v4 generated by client
  "first_name": "string",
  "last_name": "string",
  "date_of_birth": "string",           // ISO‑8601 date
  "insurance_number": "string",
  "address": "string",
  "phone": "string",
  "email": "string",
  "clinical_notes": "string"           // optional free‑text
}

**Response – Success (201)**

{
  "status": "created",
  "record_id": "string",               // UUID of stored encrypted row
  "message": "Intake record stored successfully"
}

**Error Responses**
| Code | Condition | Body |
|---|---|---|
| 400 | Validation error (missing/invalid fields) | `{"error":"validation","details":[... ]}` |
| 401 | Missing or invalid JWT | `{"error":"unauthorized"}` |
| 403 | Role not permitted to create records (e.g., front_desk creating on behalf of another patient) | `{"error":"forbidden"}` |
| 500 | Internal server error | `{"error":"internal","trace_id":"..."}` |

### 3.2 GET /api/v1/patient/{id}/pdf
**Purpose:** Retrieve a PDF summary for a patient record.

**Request Header**

- **Authorization**: Bearer <jwt-token>

**Path Parameter**
- `id`: UUID of the patient record.

**Response – Success (200)**
- `Content-Type: application/pdf`
- Body: binary PDF stream.

**Error Responses**
| Code | Condition |
|---|---|
| 401 | Unauthorized / invalid token |
| 403 | Role lacks permission to view PDF (e.g., front_desk on another clinician’s patient) |
| 404 | Record not found or access denied by RLS |
| 500 | Internal server error |

### 3.3 Audit Event POST /api/v1/audit/events (internal use)
*Not exposed externally; services POST audit events.*

{
  "event_id": "string",
  "timestamp": "string",          // ISO‑8601 with ms precision
  "actor_hash": "string",        // SHA‑256 of user identifier + salt
  "action": "CREATE|READ|UPDATE|DELETE|EXPORT",
  "resource_id": "string",
  "before_hash": "string|null",
  "after_hash": "string|null",
  "signature": "string"          // HMAC‑SHA256 using Vault key
}

## 4. Data Model Definitions

### 4.1 Patients Table (encrypted columns)
sql
CREATE TABLE patients (
    id UUID PRIMARY KEY,
    first_name BYTEA NOT NULL,
    last_name BYTEA NOT NULL,
    date_of_birth BYTEA NOT NULL,
    insurance_number BYTEA NOT NULL,
    address BYTEA,
    phone BYTEA,
    email BYTEA,
    clinical_notes BYTEA,
    created_at TIMESTAMPTZ DEFAULT now()
);
-- Row‑level security policy example:
CREATE POLICY patient_rls ON patients USING (
    current_setting('app.current_role') IN ('admin','clinician','front_desk')
);

### 4.2 Audit Log Table (append‑only)
sql
CREATE TABLE audit_log (
    event_id UUID PRIMARY KEY,
    timestamp TIMESTAMPTZ NOT NULL,
    actor_hash TEXT NOT NULL,
    action TEXT CHECK (action IN ('CREATE','READ','UPDATE','DELETE','EXPORT')),
    resource_id UUID NOT NULL,
    before_hash TEXT,
    after_hash TEXT,
    signature TEXT NOT NULL
);
-- Prevent UPDATE/DELETE on audit_log:
alter table audit_log enable row level security;
alter table audit_log add constraint immutable_check check (true);

### 4.3 TLS Encryption (in‑transit)
1. All external traffic between the browser and the API Gateway must use TLS 1.3 with forward secrecy (ECDHE).
2. Internal service‑to‑service communication (API Gateway → micro‑services) also enforces TLS 1.3 via mutual TLS (mTLS) using certificates issued by an internal HashiCorp Vault PKI.
3. Cipher suites are limited to `TLS_AES_256_GCM_SHA384` and `TLS_CHACHA20_POLY1305_SHA256`.
4. Certificate rotation occurs every 30 days; automation is defined in the Docker Compose overlay (see vault service).
5. Non‑TLS endpoints are disabled; HTTP listeners return `301` redirects to HTTPS.

### 4.5 Role‑Based Access Control (RBAC)
The system defines three roles with least‑privilege permissions:

- **admin** – Full system administrator – Create/Read/Update/Delete any patient record, manage users, view audit logs, generate PDFs.
- **clinician** – Clinical staff providing care – Create/Read/Update patient records, generate PDFs, view audit logs for own patients.

- **front_desk** – Reception staff handling intake – Create/Read patient records, generate PDFs (read‑only), view limited audit logs.

Permissions are enforced by the API Gateway using JWT claims (`role`) validated against an Open Policy Agent (OPA) policy bundle (`policy.rego`). Example OPA rule for creating a patient record:
rego
action_allowed {
\   input.method == "POST"
\   input.path == ["api","v1","intake"]
\   input.jwt.payload.role == "admin" or input.jwt.payload.role == "clinician" or input.jwt.payload.role == "front_desk"
\}

### 4.6 Immutable Audit Logging
1. Every CRUD operation on patient data triggers an Audit Service event (`/audit/events`).
2. Audit entries are stored in an append‑only PostgreSQL table (`audit_log`) with a `CHECK (operation IN ('CREATE','READ','UPDATE','DELETE','EXPORT'))` constraint.
3. Each entry includes:
   - `event_id` (UUID v4)
   - `timestamp` (ISO 8601 with millisecond precision)
   - `actor_id` (hashed user identifier)
   - `action` (enum)
   - `resource_id` (patient UUID)
   - `before_state_hash` / `after_state_hash`
   - `signature` (HMAC‑SHA256 using a vault‑managed signing key)
4. Retention policy: retain logs for **7 years**; after retention they are archived to encrypted tarballs on an air‑gapped backup volume.
5. Log integrity is verified nightly by a cron job that recomputes HMACs and alerts on mismatches.

### 4.7 Key Management & Secrets Handling
Vault is the single source of truth for all cryptographic material.
- Application containers obtain short‑lived tokens via the AppRole login flow; tokens expire after **15 minutes**.
-	Per-field keys (DEKs) rotate every 30 days, wrapped with a master key that rotates quarterly (90 days); re‑wrap is performed via Vault's `transit/rewrap` endpoint.
-	All services retrieve keys at startup and cache them in memory only.

## 5. Acceptance Criteria & Traceability Matrix
| Requirement ID | Description | Acceptance Test |
|---|---|---|
| FR-001 | Secure demographic capture via encrypted intake form | Submit valid form → record stored encrypted; verify DB column values are ciphertext; verify audit entry created |
| FR-002 | Insurance verification integration with LDAP/AD groups | Authenticate as clinician → JWT contains role claim; gateway permits access to intake endpoint |
| FR-003 | Medical history storage with field‑level encryption | Retrieve record via PDF service → decrypted fields match original input; verify no plaintext in DB dump |
| FR-004 | Automated unit and integration tests covering form validation, encryption handling, and access control | CI pipeline runs ≥80% test coverage; all tests pass on fresh container build |
| FR-005 | PDF Intake Summary Generation with watermark & timestamp | Request PDF → file contains dynamic watermark containing requestor ID and timestamp |
>
>
>
>
>
>
>
>
>
>
>
>
>
>
>
> ​
> ​
> ​
> ​
> ​
> ​
> ​
> ​
> ​
> ​
> ​
> ​
> ​
> ​
> ​
> ​
> ​
> ​
> ​
> ​
> ​
> ​
> ​
> ​
> ​
> ​
> ​
> ​
> ​
> ​
> ​
> ​
> ​
> ​
> ​
> ​
> ​
> ​
> ​
> ​
> |

# Architecture Overview: OpenAPI Contracts

## 6. System Context Diagram
mermaid
C4Container
title Patient Intake System Context
Person(admin, "Admin", "System administrator with full privileges")
Person(clinician, "Clinician", "Medical staff that can view and edit patient records")
Person(frontdesk, "Front Desk", "Staff that can create new intake records")
System_Boundary(s1, "Patient Intake System") {
    Container(web_ui, "Web UI", "React", "Patient-facing form and staff portal")
    Container(api_gateway, "API Gateway", "Envoy", "Routing, TLS termination, auth enforcement")
    Container(intake_service, "Intake Service", "FastAPI (Python)", "Validates input, encrypts fields, stores in DB")
    Container(pdf_service, "PDF Generation Service", "Node.js + wkhtmltopdf", "Creates PDF with watermark and timestamp")
    Container(audit_service, "Audit Service", "FastAPI (Python)", "Immutable audit log storage")
    Container(db, "PostgreSQL DB", "PostgreSQL 15", "Encrypted at rest, row-level security")
}
Rel(admin, web_ui, "Uses")
Rel(frontdesk, web_ui, "+ Uses")
Rel(clinician, web_ui,"Uses")
Rel(web_ui, api_gateway,"HTTPS/REST")
Rel(api_gateway,intake_service,"HTTPS/REST")
Rel(api_gateway,pdf_service,"HTTPS/REST")
Rel(api_gateway,audit_service,"HTTPS/REST")
Rel(intake_service,db,"SQL over TLS")
Rel(pdf_service,db,"Read-only SQL over TLS")
Rel(audit_service,db,"Append-only SQL over TLS")

### 7.1 Intake Service API

| Method | Path   | Description                              | Request Schema   | Response Schema   | Errors |
|--------|--------|------------------------------------------|------------------|-------------------|--------|
| POST   | /intake| Create a new patient intake record. | `IntakeRequest` | `IntakeResponse` | 400 ValidationError<br>503 VaultUnavailable |
| GET    | /intake/{id}| Retrieve an intake record (authorized). | – | `IntakeRecord` | 404 NotFound<br>401 Unauthorized |

**IntakeRequest**

- **patient_id**: string
- **demographics**: {'first_name': 'string', 'last_name': 'string', 'dob': 'date', 'ssn_encrypted': 'string'}
- **insurance**: {'provider': 'string', 'policy_number_encrypted': 'string'}
- **clinical_notes_encrypted**: string

**IntakeResponse**

- **intake_id**: uuid
- **status**: accepted
- **created_at**: timestamp

*Error handling*: When HashiCorp Vault is unavailable the service returns **503 Service Unavailable** with body `{ "error":"VaultUnavailable","detail":"Unable to retrieve encryption keys" }`. The client must retry with exponential back‑off up to five attempts.

## 8. Failure Handling & Auditing of Security Controls

| Failure Scenario               |	Detection Mechanism               |	HTTP Status |	Message to Client                                 |	Mitigation |
|---------------------------------|------------------------------------|	------------|	---------------------------------------------------|	------------|
| Vault unavailable                |	Health check probe fails           |	503         |	"Security services temporarily unavailable"      |	Exponential backoff (max 5 attempts) |
| TLS handshake failure           |	Gateway logs `tls_handshake_failure`|	400         |	"Secure connection could not be established"     |	Prompt client to retry after network fix |
| Audit Service down (circuit breaker) |	>3 consecutive errors            |	503         |	"Audit logging currently unavailable"            |	Queue events locally; flush on recovery |

## 9. Component Interaction Diagram
mermaid
flowchart TD
    subgraph Browser
        UI[Web UI]
    end
    subgraph DMZ[DMZ]
        GW[API Gateway<br>(mTLS)] --> OPA[OPA Policy Engine]
    end
    subgraph Services[Microservices]
        IS[Intake Service]
        PS[PDF Generation Service]
        AS[Audit Service]
    end
    subgraph Data[Data Stores]
        DB[(PostgreSQL DB)]
n        ES[(Elasticsearch Logs)]
n        VAULT[HashiCorp Vault]
n    end
    UI --> GW
    GW --> IS
    GW --> PS
    GW --> AS
    IS --> DB
    PS --> DB
    AS --> DB
    AS --> ES
    IS --> VAULT
    PS --> VAULT
    AS --> VAULT

All traffic is encrypted in transit using **TLS 1.3**. Services retrieve encryption keys from **Vault** via the Docker Compose `secrets:` block; secrets are never stored in source control.

## 10. Compliance Traceability

| ID      |	Description                                            |	Linked Artifact |
|---------|	------------------------------------------------------|	----------------|
| FR-001 |	Secure demographic capture (AES‑256)                 |	Intake Service request schema |
| FR-002 |	Insurance data encryption at rest                    |	Intake Service request schema |
| FR-003 |	Medical history storage encrypted                    |	DB column encryption |
| FR-004 |	Automated unit & integration tests for form validation & encryption handling |	Test plan (not shown) |
| FR-005 |	PDF Intake Summary Generation with watermark & timestamp |	PDF Generation Service |
| FR-006 |	PDF generation compliance (AES‑256)                |	PDF Generation Service |
| FR-007 |	Watermark & Timestamp requirement                    |	PDF Generation Service |
| FR-008 |	Containerized deployment isolation (Docker Compose)   |	Docker‑compose.yml (`secrets:` block) |
| FR-009 |	Docker Compose deployment orchestration               |	Docker‑compose.yml |
| FR-010 |	Comprehensive Audit Logging (new)                    |	Audit Service |
| NFR-001|	Response time <200 ms for form submissions (KPI-001)   |	API Gateway latency budget |
| NFR-002|	System uptime ≥99.9 % (KPI-002)                     |	Deployment redundancy |
| NFR-003|	Mandatory audit logging of every read/write operation (KPI-003)|	Audit Service |
| KPI-003|	Successful audit log generation for every submission verified by integration tests | |
| RISK-001|	Unauthorized data exposure mitigated by TLS 1.3 and field‑level encryption | |
| RISK-003|	Deployment misconfiguration mitigated by Docker Compose network isolation and explicit secret handling | s## 6 Security Controls Summary |
* **Zero Trust Network** – All inter‑service calls require mutual TLS.
s* **Defense in Depth** – Secrets stored in Vault; environment variables injected via Docker Compose `secrets:`; no secret in code repository.
s* **Circuit Breaker & Retry** – Configured per failure scenario above.
s* **Immutable Audit Log** – Events stored in PostgreSQL with append‑only policy and replicated to Elasticsearch for searchability.
s## 7 Open Issues & Next Steps
* Implement rate limiting on API Gateway to satisfy SOC 2 availability requirements.
s* Define backup and disaster‑recovery strategy for PostgreSQL (daily snapshots).
s* Add multi‑tenant isolation at the database schema level for SaaS tenancy support.
s---
s*Document generated by Refiner – all specifications are production‑ready and traceable to the defined requirements.*